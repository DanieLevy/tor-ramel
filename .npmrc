# Ignore optional dependencies that are platform-specific
optional = false
# Skip platform checks for optional dependencies
omit = optional
# Legacy peer deps for compatibility
legacy-peer-deps = true
# Prevent npm from failing on platform-specific packages
force = false
# Engine strict mode off
engine-strict = false

